@{
  Layout = "_Layout";
}

@* @model AudionClient.Models.Question; *@
@using System.Security.Claims
@* @model IEnumerable<AudionClient.Models.Question> *@

@model AudionClient.Models.Question

<br>
<br>
<div id="question-box" class="card" style="width:800px;">
  <img scr='~/img.logo.jpg'>
  <h3>@Html.DisplayFor(model => model.Text)</h3>
  <p style="text-align:center;">@Html.ActionLink("Add your response", "Create", "Responses", new { questionId = Model.QuestionId },new {@class="btn btn-danger"})</p>
</div>  


@* <p>@Html.ActionLink("Home", "Index", "Home")</p> *@

@* @Html.ActionLink("See all responses for this question", "Details", new { id = Model.QuestionId }) *@


@* <p>@Html.ActionLink("Add new Question", "Create")</p> *@


@* <div class="jumbotron">
<h3>@Html.DisplayFor(model => model.Text)</h3>
</div> *@

@* <p>@Model.Text</p> *@

@* <p>@Html.ActionLink("Add your response", "Create", "Responses")</p> *@

@* <p>Questions:</p> *@
@* @if (Model.Any())
{ *@
  @* <ul>
    @foreach (Question question in Model)
    {
      <li>@Html.ActionLink($"{question.Text}", "Details", new { id = question.QuestionId })</li>
    }
  </ul> *@
@* } 
else
{
  <h3>No Questions have been added yet!</h3>
} *@

